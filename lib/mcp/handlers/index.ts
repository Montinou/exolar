/**
 * MCP Handlers Index
 *
 * Exporta todos los handlers consolidados.
 * El patrón "Router" reduce 24 tools a 5 (4 nuevas + installation_config).
 */

export { handleExplore } from "./explore"
export { handleQuery } from "./query"
export { handleAction } from "./action"
export { handleDefinition } from "./definition"
